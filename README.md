# Facilita Capital

Sitio de Facilita Capital: financiamiento al ritmo de los negocios.

```bash
npm install
npm run dev
```

El formulario de leads se guarda en `data/leads.json` (en desarrollo) y también en `localStorage`.
